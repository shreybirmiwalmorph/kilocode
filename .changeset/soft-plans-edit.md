---
"@kilocode/cli": patch
---

Allow Architect mode and plan-like custom agents to save markdown plan files when session edit permissions are disabled, while keeping planning agents restricted to plan files.
